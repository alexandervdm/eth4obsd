#!/bin/ksh
#
# $OpenBSD$

daemon="${TRUEPREFIX}/bin/beacon-chain"
daemon_user="_prysm"
daemon_flags=""

. /etc/rc.d/rc.subr

rc_bg=YES
rc_reload=NO

rc_start() {
	${rcexec} "${daemon} ${daemon_flags} 2>> ${VARBASE}/prysm/beacon-chain.log"
}

rc_cmd $1
